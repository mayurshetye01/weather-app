package com.gloresoft.weatherapp.backend.security.authentication;

import org.springframework.security.core.userdetails.UserDetailsService;

// Marker as of now, can add additional methods going forward if required
public interface UserAuthenticationService extends UserDetailsService {
}
